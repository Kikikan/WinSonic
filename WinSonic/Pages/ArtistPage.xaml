<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinSonic.Pages.ArtistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinSonic.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:api="using:WinSonic.Model.Api"
    xmlns:control="using:WinSonic.Pages.Control"
    Loaded="Page_Loaded">

    <Page.Resources>
        <CollectionViewSource x:Name="ArtistsCVS" IsSourceGrouped="True"/>
        <DataTemplate x:Key="ArtistListViewTemplate" x:DataType="api:DetailedArtist">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image x:Name="Image" Width="100" Stretch="Uniform" Margin="0 10 0 10">
                    <Image.Source>
                        <BitmapImage UriSource="{x:Bind SmallImageUri}"/>
                    </Image.Source>
                </Image>
                <TextBlock Grid.Column="1"
                    Text="{x:Bind Name}"
                    x:Phase="1"
                    Style="{ThemeResource BaseTextBlockStyle}"
                    Margin="12,6,0,0" VerticalAlignment="Center"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="25 0 0 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Artists" Style="{StaticResource TitleTextBlockStyle}" Margin="0 25 0 25"/>
        <TextBlock x:Name="ArtistNameTextBlock" Grid.Row="0" Grid.Column="1" Style="{StaticResource TitleTextBlockStyle}" Margin="0 25 0 25"/>
        
        <ListView x:Name="ArtistListView" Grid.Row="1" Grid.Column="0" ItemsSource="{x:Bind ArtistsCVS.View, Mode=OneWay}" ItemTemplate="{StaticResource ArtistListViewTemplate}" SelectionChanged="ArtistListView_SelectionChanged">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel AreStickyGroupHeadersEnabled="True"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.GroupStyle>
                <GroupStyle >
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="local:GroupInfoList">
                            <Border AutomationProperties.AccessibilityView="Raw">
                                <TextBlock Text="{x:Bind Key}" Style="{ThemeResource SubtitleTextBlockStyle}" AutomationProperties.AccessibilityView="Raw"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="BiographyTextBlock" Grid.Row="0" TextWrapping="WrapWholeWords" Margin="0 0 0 10"/>
            <control:PictureControlPage x:Name="AlbumsPage" Grid.Row="1"/>
        </Grid>
        
    </Grid>
</Page>
