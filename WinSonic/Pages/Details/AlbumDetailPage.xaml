<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinSonic.Pages.AlbumDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinSonic.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="headerContent" Margin="20,52,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Connected image -->
            <Image x:Name="detailedImage" MaxHeight="400" VerticalAlignment="Top" Stretch="Uniform">
                <Image.Source>
                    <BitmapImage UriSource="{x:Bind DetailedObject.IconUri}"/>
                </Image.Source>
            </Image>

            <!-- Panel to be coordinated with connected animation -->
            <StackPanel x:Name="coordinatedPanel" Grid.Column="1" VerticalAlignment="Top" Margin="20,0">
                <TextBlock Text="{x:Bind DetailedObject.Title}" Style="{ThemeResource SubheaderTextBlockStyle}" Margin="0,0,0,10"  />
                <TextBlock Text="{x:Bind DetailedObject.Subtitle}" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Panel containing more info, to be shown after the connected animation. -->
        <Grid x:Name="moreInfoPanel" Grid.Row="1" Margin="10">
            <TextBlock Text="Description" Style="{ThemeResource SubtitleTextBlockStyle}"  />
        </Grid>

    </Grid>
</Page>
