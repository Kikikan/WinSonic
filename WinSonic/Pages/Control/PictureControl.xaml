<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinSonic.Pages.Control.PictureControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinSonic.Pages.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ControlGrid" Padding="8" PointerPressed="ControlGrid_PointerPressed">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" x:Name="Image" Height="200" Stretch="Uniform">
            <Image.Source>
                <BitmapImage UriSource="{x:Bind IconUri}"/>
            </Image.Source>
        </Image>
        <FontIcon x:Name="FavouriteIcon1" Glyph="&#xEB52;" Foreground="Red" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <FontIcon x:Name="FavouriteIcon2" Glyph="&#xEB51;" Foreground="Black" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <StackPanel Grid.Row="1">
            <TextBlock x:Name="TitleTextBlock" Text="{x:Bind Title}" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
            <TextBlock x:Name="SubtitleTextBlock" Text="{x:Bind Subtitle}" TextTrimming="CharacterEllipsis" />
        </StackPanel>
    </Grid>
</UserControl>
